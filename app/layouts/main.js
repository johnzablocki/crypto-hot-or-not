import { Grid, Row, Col } from 'react-bootstrap';

const MainLayout = () => (
    <Grid>
        <Row>
            <Col lg={8} lgOffset={2}>Foo</Col>
        </Row>
    </Grid>
);

export default MainLayout;
